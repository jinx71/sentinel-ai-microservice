# Example only. Create the real Secret out-of-band and DO NOT commit it:
#   kubectl -n sentinel create secret generic sentinel-secrets \
#     --from-literal=ANTHROPIC_API_KEY=sk-ant-...
apiVersion: v1
kind: Secret
metadata:
  name: sentinel-secrets
  namespace: sentinel
  labels:
    app.kubernetes.io/name: sentinel-ai-microservice
type: Opaque
stringData:
  ANTHROPIC_API_KEY: "replace-me-or-leave-empty"
